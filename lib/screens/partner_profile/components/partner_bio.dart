import 'package:flutter/material.dart';

class PartnerBio extends StatelessWidget {
  const PartnerBio({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
